# dsh-repeat-guard 的 bundle 补丁：把本插件插进宿主配置。
#
# 本文件由 package.json 的 dsh.bundle.patch 字段声明，安装后由 dsh 自动加载，
# 装在 profile 里的用户不需要改自己的 cordis.patch.yml。
- insert:
    - id: dsh-repeat-guard
      name: dsh-repeat-guard
